import React from 'react';
import ReactSlickIntegration from '../../components/zoom/ReactSlickIntegration';

const ReactSlickInt = () => (
  <div>
    <ReactSlickIntegration />
  </div>
);

export default ReactSlickInt;
